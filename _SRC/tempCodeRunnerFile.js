/ 		questionClose = questionForm.querySelector('.close'),
		// 		workArray = [sendLink, questionClose];

		// 		for(let i = 0; i < workArray.length; i++) {
		// 			workArray[i].addEventListener('click', function(e) {
		// 				e.preventDefault();
		// 				questionForm.classList.toggle('active');
		// 				bodyBack.classList.toggle('active');
		// 				document.body.classList.toggle('block');
		// 			});		
		// 		}